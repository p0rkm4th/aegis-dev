# Data model

The durable model will distinguish principals, Vaults, Spaces, memberships,
objectives, runs, actions, observations, verification, results, and audit
events. Conversation context and vector retrieval are not canonical state.
