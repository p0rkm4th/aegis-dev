# {{ pack_id }} Pack

Version: `{{ version }}`

This skeleton was rendered from the AEGIS Pack Forge structural contract.
It contains declarations and fail-closed runtime seams only. It is not installed,
enabled, authorized, or connected to an external service by Forge.

The domain owner must implement canonical state, executor behavior, verifier
behavior, persistence, integrations, and lifecycle approval through existing AEGIS
interfaces.
