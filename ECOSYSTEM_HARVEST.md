# Ecosystem harvest

Current design references are interface-level only. OpenClaw compatibility is
pinned to the tested `2026.8.1` release train and its documented Gateway/RPC,
plugin SDK, tool-policy, sandbox, and approval surfaces. Future donor use must
record whether it is design harvest, interface only, or a permissively licensed
dependency.
