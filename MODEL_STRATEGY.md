# Model strategy

Models are replaceable cognition. Deterministic paths run first. Model-facing
capabilities are bounded and structured decisions are validated. Ollama with an
approximately 8B Qwen-class model is the first local baseline; models never own
authorization, truth, persistence, or completion.
